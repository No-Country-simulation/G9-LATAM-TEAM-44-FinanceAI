# FinanceAI · ml-service — dependencias de ejecucion
#
# Solo lo necesario para levantar el contrato. Las librerias de entrenamiento
# (pandas, scikit-learn, jupyter) viven en ciencia-datos/requirements.txt y NO
# se instalan aqui: la imagen del servicio debe quedar liviana.
#
# Cuando entren los modelos reales, agregar aqui unicamente lo que se necesita
# para CARGARLOS e inferir (p. ej. scikit-learn + joblib, u onnxruntime).

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
